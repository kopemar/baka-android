<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <View
            android:id="@+id/subtoolbar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/base_subtoolbar_height"
            android:background="@color/very_light_blue"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@color/light_blue"
            app:layout_constraintTop_toBottomOf="@id/subtoolbar" />

        <ImageView
            android:id="@+id/iv_icon"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:background="@drawable/circle"
            android:padding="20dp"
            android:src="@drawable/ic_calendar"
            app:layout_constraintBottom_toBottomOf="@id/subtoolbar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/subtoolbar"
            app:tint="@color/sunny_evenings" />

        <Button
            android:id="@+id/btn_test"
            style="@style/Button.Primary"
            android:layout_width="match_parent"
            android:layout_margin="16dp"
            android:text="Test Api Call"
            app:layout_constraintTop_toBottomOf="@id/iv_icon" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/btn_test"
            >
            <include layout="@layout/list_shift" android:layout_height="100dp" android:layout_width="match_parent" android:layout_margin="16dp"/>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>